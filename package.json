{
  "name": "vc-job-scrapers",
  "version": "2.0.0",
  "private": true,
  "description": "Cloudflare Worker that scrapes VC portfolio job boards and returns contract JSON. Called by an n8n orchestrator.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/*.test.js",
    "test:update": "UPDATE_SNAPSHOTS=1 node --test test/*.test.js",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.84.1"
  }
}
