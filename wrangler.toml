name = "vc-job-scrapers"
main = "src/index.js"
compatibility_date = "2026-04-22"

[observability]
enabled = true

# Exposes the deployment id to /healthz so a push can be seen to have landed.
[version_metadata]
binding = "CF_VERSION_METADATA"
